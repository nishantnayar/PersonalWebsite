/** @type {import('next').NextConfig} */
const nextConfig = {
  // Hide the Next.js dev-mode indicator badge in the corner.
  devIndicators: false,
  images: {
    // Add any external image domains here if needed
    // e.g. remotePatterns: [{ hostname: 'github.com' }]
  },
};

module.exports = nextConfig;
